.DS_Store
node_modules/
*.log

# Other agent roots the Skills CLI may create locally.
# Project install: .agents/skills/<name> → ../../skills/<name>;
# .claude/skills/<name> → ../../.agents/skills/<name>.
.cursor/
.codex/
.goose/
.opencode/
.openclaw/
.skills-lock.json
agent/

# Human-only debug notes (not shipped with the skill)
debug/
