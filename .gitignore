.DS_Store
node_modules/
*.log

# Other agent roots the Skills CLI may create locally.
# Canonical project install is .agents/skills/ (plus .claude/skills/ symlinks).
.cursor/
.codex/
.goose/
.opencode/
.openclaw/
.skills-lock.json
agent/

# Human-only debug notes (not shipped with the skill)
debug/
